apiVersion: v1
kind: Service
metadata:
  name: demo-load
  namespace: demo
  labels:
    app.kubernetes.io/name: demo-load
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-load
  ports:
    - name: http
      port: 8080
      targetPort: http
