# After the cluster exists: one command
#   helmfile -f helmfile/helmfile.yaml apply
# CRDs: presync hook (kubectl apply). Later stacks add a file under releases/.

helmDefaults:
  wait: true
  timeout: 600
  createNamespace: true

bases:
  - releases/traefik.yaml
