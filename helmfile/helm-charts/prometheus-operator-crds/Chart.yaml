apiVersion: v2
name: prometheus-operator-crds
description: Prometheus Operator CRDs only (monitoring.coreos.com). Slim extract from prometheus-operator-crds 31.0.1 / Operator v0.93.1 — plain YAML for kubectl apply via presync hook so the Helm release Secret stays under 1MiB.
type: application
version: 31.0.1
appVersion: "v0.93.1"
kubeVersion: ">=1.16.0-0"
sources:
  - https://github.com/prometheus-community/helm-charts
